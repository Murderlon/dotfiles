#!/usr/bin/env bash

stow -vt ~ git tmux nvim zsh fish starship ghostty

for script in scripts/*; do
  [ -f "$script" ] || continue
  sudo ln -sf "$PWD/$script" "/usr/local/bin/$(basename "$script")"
done
