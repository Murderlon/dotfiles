- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore
    ~/.macOS.sh: .macOS.sh
    ~/.shell.sh: .shell.sh
    ~/.config/nvim/init.vim: init.vim
